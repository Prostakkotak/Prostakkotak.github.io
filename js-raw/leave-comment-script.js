(function () {
  var leaveCommentSend = document.getElementById('leave-comment__send');

  leaveCommentSend.onclick = function () {
    alert('Так как сайт без движка и ДБ, комменты не работают (Раньше здесь был код на JS, имитирующий добавление комментария, но при обновлении страницы все исчезало и поэтому смысла в нем особо то и не было)');
  }
}())
